<UserControl x:Class="DsmSuite.DsmViewer.View.Matrix.MatrixMetricsSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Button Content="{StaticResource LeftArrow}" Style="{StaticResource ArrowButton}" ToolTip="Select previous metric" Command="{Binding PreviousMetricCommand}"/>
        <TextBlock Text="{Binding SelectedMetricTypeName}" Foreground="{StaticResource TextColor}" Width="70" TextWrapping="Wrap" TextAlignment="Center"/>
        <Button Content="{StaticResource RightArrow}" Style="{StaticResource ArrowButton}" ToolTip="Select next metric" Command="{Binding NextMetricCommand}"/>
    </StackPanel>
</UserControl>
